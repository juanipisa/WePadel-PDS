/**
 * BACKEND — Nucleo del TP (POO + patrones de diseno).
 * <p>
 * Contiene el modelo de dominio, reglas de negocio y la Facade.
 * La logica de compras, estados, pagos y notificaciones vive aqui.
 * La carpeta {@code persistencia/} solo guarda y carga datos en MySQL.
 */
package com.uade.tpo.wepadel.backend;
