/**
 * PERSISTENCIA — Capa tecnica JPA + MySQL (Fase 2).
 * <p>
 * Entidades de BD, repositorios, mappers y services de guardado/carga.
 * No contiene reglas de negocio: delega al paquete {@code backend/}.
 * Los datos iniciales se cargan directamente en MySQL (no hay seed en codigo).
 */
package com.uade.tpo.wepadel.persistencia;
